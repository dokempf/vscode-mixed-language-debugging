{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "GDB attached to Python process",
      "type": "cppdbg",
      "request": "attach",
      "program": "${command:python.interpreterPath}",
      "processId": "${input:python-pid}",
      "MIMode": "gdb",
      "setupCommands": [
        {
          "description": "Enable pretty-printing for gdb",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        },
        {
          "description": "Set Disassembly Flavor to Intel",
          "text": "-gdb-set disassembly-flavor intel",
          "ignoreFailures": true
        }
      ]
    },
    {
      "name": "Debug pytest",
      "type": "debugpy",
      "request": "launch",
      "module": "pytest",
      "args": [
        "-s",
        // so stdout shows in debug console
      ],
      "console": "integratedTerminal",
      "justMyCode": false
    }
  ],
  "inputs": [
    {
      "id": "python-pid",
      "type": "command",
      "command": "shellCommand.execute",
      "args": {
        "command": "pgrep -f python | tail -n 2 | head -n 1",
        "useSingleResult": true
      }
    }
  ]
}
